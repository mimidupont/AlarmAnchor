// Shown on the session screen, so a boat-vs-remote version mismatch is
// diagnosable at a glance. Keep in step with versionName in
// android/app/build.gradle — that is what testers see in App Tester.
//
// The build number beside it in Firebase is the versionCode, derived from
// the git commit count, and moves on its own.
export const APP_VERSION = '1.1.0';
